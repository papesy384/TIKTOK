# Optional: API URL for feed videos (GET returns { videos: VideoItem[], nextCursor?: string })
# When unset, no request is made and fallback is used.
# NEXT_PUBLIC_VIDEOS_API_URL=https://api.example.com/videos

# Optional: JSON array of fallback VideoItem when API is unset or fails.
# Example: [{"id":"1","src":"/videos/a.mp4","username":"@user","description":"Caption"}]
# NEXT_PUBLIC_FALLBACK_VIDEOS=[]

# --- Video hosting (Cloudinary / Mux) ---
# Cloudinary: used when API returns cloudinaryPublicId. Builds optimized MP4 URLs (f_auto, q_auto).
# NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your_cloud_name

# Mux: API can return muxPlaybackId; app resolves to HLS (stream.mux.com). No env needed for public playback.
# Signed playback: implement token endpoint and set src with ?token=...

# Optional: engagement service URL. When set, POST /api/engage forwards each like/unlike to this URL.
# ENGAGEMENT_SERVICE_URL=https://your-api.com/events/engage
